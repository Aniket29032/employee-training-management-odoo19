<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_training_trainers" model="ir.actions.act_window">
        <field name="name">Trainers</field>
        <field name="res_model">hr.employee</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('is_trainer','=',True)]</field>
    </record>

    <record id="action_training_trainees" model="ir.actions.act_window">
        <field name="name">Trainees</field>
        <field name="res_model">hr.employee</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('is_trainee','=',True)]</field>
    </record>

</odoo>